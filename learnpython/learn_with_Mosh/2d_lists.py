matrix = [
    [1,2,3],
    [4,5,6],
    [7,8,9]
]

#print(matrix)

# The list inside the list - this is called 2d-list.
# to access the item inside the child list, we use index, like below

# to print, item 3 in first row, as below

#print(matrix[0][2])

# using the same way,we can update/modify the item in matrix

matrix[0][2] = 12

print(matrix[0][2])